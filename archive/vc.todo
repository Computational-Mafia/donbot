So the votecounter has two big components. Maybe 3. 
- The function that processes posts and returns any detected votes
- The function that maintains a votecounter object and updates it with new votes